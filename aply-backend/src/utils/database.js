// Utility for Firestore interactions.
// Functions: Connect to DB, save user resume data, retrieve user data, save application history.
// Use Firebase Admin SDK methods like db.collection('users').doc(userId).set(data).